import 'package:flutter/material.dart';

class BrandName extends StatelessWidget {
  const BrandName({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return Text('JobUp', style: TextStyle(color: Colors.pink, fontSize: 30, fontFamily: 'Hind'));
  }
}
